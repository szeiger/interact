cons Z
cons S(n)

def add(_, y): r
  | Z => y
  | S(x) => add(x, S(y))

def mult(_, y): r
  | Z => erase(y), Z
  | S(x) => (a, b) = dup(y),
            r = add(b, mult(x, a))

let res = mult(1000'c, 1000'c)
